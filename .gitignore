# Server data: the database and the generated seed artwork are reproducible
# with `node seed.js`, so they do not belong in the repository.
server/data/

# Secrets
.env
server/.env

# The Expo app keeps its own ignores in mobile/.gitignore.
node_modules/

# macOS
.DS_Store
